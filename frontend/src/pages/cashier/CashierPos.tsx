import { Box, Typography } from '@mui/material'

export default function CashierPos() {
    return (
        <Box>
            <Typography variant="h5" mb={2}>Terminal POS (Cashier)</Typography>
            <Typography color="text.secondary">Aquí irá la combinación de ProductCatalog y el carrito lado a lado para cobrar rápido.</Typography>
        </Box>
    )
}
